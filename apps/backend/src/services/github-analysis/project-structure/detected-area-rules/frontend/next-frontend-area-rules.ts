import type { DetectedAreaRuleContext } from '../../project-structure-detected-areas.types';

/**
 * Detects Next.js frontend application areas from path-only repository evidence.
 * Internal scoring rules are intentionally left for the next step.
 */
export function addNextFrontendAreas({
  entries,
  candidates,
}: DetectedAreaRuleContext) {
  
}
