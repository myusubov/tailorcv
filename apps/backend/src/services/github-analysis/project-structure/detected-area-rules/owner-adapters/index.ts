/**
 * Barrel for owner adapters — the per-shape resolvers that map a matched
 * signal's evidence path to its area owner root. Detectors and the declarative
 * area-rule engine import adapters from here by name.
 */
export { resolveUnitRootOwner } from './resolve-unit-root-owner';
