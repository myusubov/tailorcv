import type { DetectedAreaRuleContext } from '../../project-structure-detected-areas.types';

/**
 * Adds generic `Backend API` candidates after framework-specific detectors.
 */
export function addGenericBackendAreas(
  _context: DetectedAreaRuleContext,
): void {}
