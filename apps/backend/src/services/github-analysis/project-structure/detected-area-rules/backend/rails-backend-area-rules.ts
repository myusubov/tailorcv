import type { DetectedAreaRuleContext } from '../../project-structure-detected-areas.types';

/**
 * Adds `Backend API` candidates from Ruby on Rails path evidence.
 */
export function addRailsBackendAreas(_context: DetectedAreaRuleContext): void {}
